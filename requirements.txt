# Web app
fastapi>=0.110
uvicorn[standard]>=0.27

# Instagram data backends (either one is enough - see doc/web-ui.md)
hikerapi==1.8.1
instagrapi>=2.0

# Local LLM client, for AI mode (Base mode doesn't need it)
ollama>=0.3

# Reverse-geocoding the locations tagged on posts
geopy>=2.0.0
